<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h2>Sasta quora</h2>
    <ul>
       <% for(post of posts){ %>
      <div class="post">
        <h4>@<i><b><%= post.username %></b></i></h4>
        <p> <%= post.content %></p>
        <button><a href="http://localhost:8080/posts/<%= post.id %>">see more</a></button>
        <button><a href="http://localhost:8080/posts/<%= post.id %>/edit">edit post</a></button>
        <form method="post"  action="/posts/<%= post.id %>?_method=DELETE">  <button>delete</button></form>
     </div>

     
        <% } %>
    </ul>
   <button> <a href="http://localhost:8080/posts/new">create new post</a></button>
</body>
</html>